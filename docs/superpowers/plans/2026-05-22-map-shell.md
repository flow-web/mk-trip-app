# Map Shell Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refondre la page Map d'un voyage en shell immersif "à la Wanderlog" : carte plein écran, day filter dock, bottom sheet draggable, sheet détail spot au clic — sans toucher à la navigation globale ni aux autres écrans.

**Architecture:** Un orchestrateur `MapShell` tient le state `{ selectedDayId, selectedSpotId }`, distribue aux sous-composants (`MapView` refondu en plein écran, `MapDayDock` flottant haut, `MapSpotSheet` Vaul 3 snap points, `MapSpotDetailSheet` overlay). Logique pure (palette jours, filtres) extraite dans `lib/map/` avec tests unitaires Vitest. Tests E2E Playwright pour les flux critiques. Feature flag env var pour rollback facile.

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript · Tailwind · Mapbox GL via react-map-gl 7.1 · Vaul 1.1 · Dexie 4 · Supabase · Vitest 4 · Playwright (à installer)

**Spec:** `docs/superpowers/specs/2026-05-22-map-shell-design.md`

**Ajustements vs spec après inventaire code :**
1. Spec assumait `spots.day_id` mais la colonne n'existe pas → **migration Supabase + bump Dexie v2** ajoutée
2. Spec disait "Mapbox layers natifs (circle + symbol)" → **on garde `<Marker>` React** (volume actuel ne justifie pas le refactor, on switchera si perf observée)
3. Spec disait `vaul` "pressentie" → **confirmé `vaul@1.1.1`** déjà installé et utilisé
4. **Acceptance #5 "Marquer visité" reportée** → `activity_completions` est lié aux activities, pas aux spots. Implémenter "visité" sur un spot nécessite soit une jointure spot→activity, soit une nouvelle table `spot_completions`. Décision schema dépasse le scope UX du sous-projet A → reportée à un micro sous-projet futur. Spec amendé en conséquence.

---

## Table des tâches

| # | Tâche | Type | Estimation |
|---|---|---|---|
| 1 | Setup branche + Playwright | Setup | 30 min |
| 2 | Migration Supabase + Dexie bump v2 | Migration | 45 min |
| 3 | Vitest jsdom env (pour composants futurs) | Setup | 15 min |
| 4 | `lib/map/dayColors.ts` (palette + tests) | Pure logic TDD | 30 min |
| 5 | `lib/map/spotFilters.ts` (visibleSpots/Lines + tests) | Pure logic TDD | 45 min |
| 6 | Hook `useTripMapData` (spots + days) | Hook | 45 min |
| 7 | Composant `MapDayDock` | UI | 1 h |
| 8 | Refonte `MapView` (Markers colorés + lignes) | UI | 1 h 30 |
| 9 | Composant `MapSpotDetailSheet` | UI | 1 h |
| 10 | Refonte `MapSpotSheet` (snap points + liste filtrée) | UI | 1 h |
| 11 | Composant orchestrateur `MapShell` | UI | 45 min |
| 12 | Remplacer `app/trips/[tripId]/map/page.tsx` + feature flag | Integration | 30 min |
| 13 | E2E Playwright (4 scénarios acceptance) | Tests | 1 h 30 |
| 14 | Acceptance manuel + preview Vercel | Validation | 30 min |

**Total estimé : ~10 h de travail focused → ~5-7 jours calendaires en réel.**

---

### Task 1: Setup branche + Playwright

**Files:**
- Create: `playwright.config.ts`
- Modify: `package.json` (deps + scripts)
- Modify: `.gitignore` (ajouter `test-results/`, `playwright-report/`)

- [ ] **Step 1.1: Créer la branche de travail**

```bash
git checkout -b feat/map-shell-v2
```

Expected: `Switched to a new branch 'feat/map-shell-v2'`

- [ ] **Step 1.2: Installer Playwright**

```bash
npm install --save-dev @playwright/test
npx playwright install chromium
```

Expected: Chromium downloaded, ~150 MB.

- [ ] **Step 1.3: Créer `playwright.config.ts` à la racine**

```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'mobile',
      use: { ...devices['iPhone 13'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
```

- [ ] **Step 1.4: Ajouter scripts npm**

Modifier `package.json` section `scripts` :

```json
{
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  }
}
```

- [ ] **Step 1.5: Update `.gitignore`**

Ajouter à la fin de `.gitignore` :

```
# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
```

- [ ] **Step 1.6: Créer le dossier `e2e/` avec un smoke test**

Create `e2e/smoke.spec.ts`:

```typescript
import { test, expect } from '@playwright/test'

test('app loads', async ({ page }) => {
  await page.goto('/')
  await expect(page).toHaveTitle(/MK Trip|Trip/i)
})
```

- [ ] **Step 1.7: Vérifier que Playwright tourne**

```bash
npm run test:e2e -- --project=chromium
```

Expected: Test "app loads" PASS. Si fail, vérifier que `npm run dev` démarre OK.

- [ ] **Step 1.8: Commit**

```bash
git add playwright.config.ts package.json package-lock.json .gitignore e2e/smoke.spec.ts
git commit -m "chore(test): installer Playwright + smoke E2E + scripts npm"
```

---

### Task 2: Migration Supabase day_id sur spots + bump Dexie v2

**Files:**
- Create: `supabase/migrations/20260522000000_add_day_id_to_spots.sql`
- Modify: `lib/db/index.ts` (bump version + ajouter day_id à l'index)
- Modify: `lib/db/schema.ts` (s'assurer que LocalSpot inclut day_id — il est déjà dans Tables['spots']['Row'] via les types Supabase une fois la migration appliquée, mais on doit régénérer les types)

- [ ] **Step 2.1: Créer la migration SQL**

Create `supabase/migrations/20260522000000_add_day_id_to_spots.sql`:

```sql
-- Ajoute day_id (nullable) sur spots pour permettre de filtrer la carte par jour.
-- Nullable car un spot peut exister sans être rattaché à un jour spécifique (mode "Tous").

alter table public.spots
  add column day_id uuid references public.days(id) on delete set null;

create index idx_spots_day on public.spots(day_id);

comment on column public.spots.day_id is
  'Optional link to a specific day. When set, the spot appears in the day filter; when null, only visible in "Tous" view.';
```

- [ ] **Step 2.2: Appliquer la migration en local (si Supabase local) OU sur projet distant**

Si Supabase CLI local utilisé :
```bash
npx supabase db reset
```

Sinon, appliquer via le dashboard Supabase ou via `supabase db push`. Vérifier dans le SQL editor :

```sql
select column_name, data_type, is_nullable
from information_schema.columns
where table_name = 'spots' and table_schema = 'public';
```

Expected: `day_id | uuid | YES`

- [ ] **Step 2.3: Régénérer les types TypeScript Supabase**

```bash
npx supabase gen types typescript --local > lib/supabase/types.ts
```

OU si projet distant : utiliser le flag `--project-id <id>`.

Vérifier que `Database['public']['Tables']['spots']['Row']` contient maintenant `day_id: string | null`.

- [ ] **Step 2.4: Bump Dexie à v2 avec day_id indexé**

Modifier `lib/db/index.ts`:

```typescript
constructor() {
  super('mk_trip')
  this.version(1).stores({
    profiles: 'id, display_name',
    trips: 'id, owner_id, trip_type, name',
    trip_members: '[trip_id+user_id], trip_id, user_id',
    days: 'id, trip_id, day_number',
    activities: 'id, day_id, position',
    activity_completions: '[activity_id+user_id], activity_id, user_id',
    spots: 'id, trip_id, category',
    expenses: 'id, trip_id, payer_id, spent_at',
    expense_splits: '[expense_id+user_id], expense_id, user_id',
    checklist_items: 'id, trip_id, category, position',
    checklist_completions: '[item_id+user_id], item_id, user_id',
    guide_cards: 'id, trip_id, position',
    sync_queue: 'id, status, created_at, row_id',
    pending_uploads: 'id, trip_id, status, created_at',
  })
  this.version(2).stores({
    spots: 'id, trip_id, category, day_id',
  })
}
```

Note Dexie : seuls les stores qui changent sont à redéclarer dans `.version(2)`.

- [ ] **Step 2.5: Lancer l'app pour vérifier que la migration Dexie passe sans erreur**

```bash
npm run dev
```

Ouvrir `http://localhost:3000`, ouvrir un voyage, regarder la console (F12). Aucune erreur Dexie de type "UpgradeError" attendue.

- [ ] **Step 2.6: Commit**

```bash
git add supabase/migrations/20260522000000_add_day_id_to_spots.sql lib/db/index.ts lib/supabase/types.ts
git commit -m "feat(db): ajouter day_id nullable sur spots + Dexie v2"
```

---

### Task 3: Vitest jsdom environment

**Files:**
- Modify: `vitest.config.ts`
- Modify: `package.json` (ajouter jsdom + testing-library si on veut tester des composants)

- [ ] **Step 3.1: Installer jsdom + testing-library**

```bash
npm install --save-dev jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

- [ ] **Step 3.2: Mettre à jour `vitest.config.ts`**

Replace contents of `vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config'
import path from 'node:path'

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['lib/**/*.test.ts', 'lib/**/*.test.tsx', 'components/**/*.test.tsx'],
    setupFiles: ['./vitest.setup.ts'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, '.') },
  },
})
```

- [ ] **Step 3.3: Créer le setup file**

Create `vitest.setup.ts` à la racine :

```typescript
import '@testing-library/jest-dom/vitest'
```

- [ ] **Step 3.4: Vérifier que les tests existants passent toujours**

```bash
npm test -- --run
```

Expected: tous les tests existants (notamment `lib/segments/__tests__/geo.test.ts`) PASS.

- [ ] **Step 3.5: Commit**

```bash
git add vitest.config.ts vitest.setup.ts package.json package-lock.json
git commit -m "chore(test): vitest env jsdom + testing-library pour tests composants"
```

---

### Task 4: `lib/map/dayColors.ts` (palette + tests)

**Files:**
- Create: `lib/map/dayColors.ts`
- Test: `lib/map/dayColors.test.ts`

- [ ] **Step 4.1: Écrire les tests d'abord (TDD)**

Create `lib/map/dayColors.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { getDayColor, DAY_PALETTE } from './dayColors'

describe('dayColors', () => {
  it('exposes a fixed palette of exactly 8 colors', () => {
    expect(DAY_PALETTE).toHaveLength(8)
    DAY_PALETTE.forEach((c) => expect(c).toMatch(/^#[0-9a-f]{6}$/i))
  })

  it('returns the first color for day index 0', () => {
    expect(getDayColor(0)).toBe(DAY_PALETTE[0])
  })

  it('returns the 8th color for day index 7', () => {
    expect(getDayColor(7)).toBe(DAY_PALETTE[7])
  })

  it('cycles back to the first color for day index 8 (9th day)', () => {
    expect(getDayColor(8)).toBe(DAY_PALETTE[0])
  })

  it('cycles correctly for high indices', () => {
    expect(getDayColor(15)).toBe(DAY_PALETTE[7])
    expect(getDayColor(16)).toBe(DAY_PALETTE[0])
  })

  it('clamps negative or NaN indices to 0', () => {
    expect(getDayColor(-1)).toBe(DAY_PALETTE[0])
    expect(getDayColor(NaN)).toBe(DAY_PALETTE[0])
  })
})
```

- [ ] **Step 4.2: Run tests, vérifier qu'ils échouent**

```bash
npm test -- --run lib/map/dayColors.test.ts
```

Expected: FAIL avec "Cannot find module './dayColors'".

- [ ] **Step 4.3: Implémenter le module**

Create `lib/map/dayColors.ts`:

```typescript
// Palette fixe de 8 couleurs distinctes pour identifier les jours sur la carte.
// Indépendante de l'accent voyage : cohérence cross-voyages.
// Choisies pour être bien distinctes en clair ET en sombre, et lisibles
// par-dessus un fond Mapbox light/dark.
export const DAY_PALETTE = [
  '#ef4444', // red-500
  '#f59e0b', // amber-500
  '#10b981', // emerald-500
  '#3b82f6', // blue-500
  '#a855f7', // purple-500
  '#ec4899', // pink-500
  '#14b8a6', // teal-500
  '#f97316', // orange-500
] as const

export function getDayColor(dayIndex: number): string {
  if (!Number.isFinite(dayIndex) || dayIndex < 0) return DAY_PALETTE[0]
  return DAY_PALETTE[Math.floor(dayIndex) % DAY_PALETTE.length]
}
```

- [ ] **Step 4.4: Run tests, vérifier qu'ils passent**

```bash
npm test -- --run lib/map/dayColors.test.ts
```

Expected: 6 tests PASS.

- [ ] **Step 4.5: Commit**

```bash
git add lib/map/dayColors.ts lib/map/dayColors.test.ts
git commit -m "feat(map): palette fixe 8 couleurs pour jours + getDayColor"
```

---

### Task 5: `lib/map/spotFilters.ts` (filtres + tests)

**Files:**
- Create: `lib/map/spotFilters.ts`
- Test: `lib/map/spotFilters.test.ts`

- [ ] **Step 5.1: Écrire les tests d'abord**

Create `lib/map/spotFilters.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { filterVisibleSpots, computeDayLines, type MapSpot } from './spotFilters'

const spots: MapSpot[] = [
  { id: 's1', name: 'A', lat: 38.7, lng: -9.1, category: 'food', day_id: 'd1', time: '09:00' },
  { id: 's2', name: 'B', lat: 38.71, lng: -9.11, category: 'culture', day_id: 'd1', time: '12:00' },
  { id: 's3', name: 'C', lat: 38.72, lng: -9.12, category: 'nature', day_id: 'd2', time: '10:00' },
  { id: 's4', name: 'D', lat: 38.73, lng: -9.13, category: 'sport', day_id: null, time: null },
]

describe('filterVisibleSpots', () => {
  it('returns all spots when selectedDayId is "all"', () => {
    expect(filterVisibleSpots(spots, 'all')).toEqual(spots)
  })

  it('returns only spots of the selected day', () => {
    const result = filterVisibleSpots(spots, 'd1')
    expect(result.map((s) => s.id)).toEqual(['s1', 's2'])
  })

  it('excludes spots without day_id when a specific day is selected', () => {
    const result = filterVisibleSpots(spots, 'd2')
    expect(result.map((s) => s.id)).toEqual(['s3'])
  })

  it('returns empty array if no spot matches', () => {
    expect(filterVisibleSpots(spots, 'nonexistent')).toEqual([])
  })

  it('handles empty input', () => {
    expect(filterVisibleSpots([], 'all')).toEqual([])
  })
})

describe('computeDayLines', () => {
  it('returns no lines when selectedDayId is "all"', () => {
    expect(computeDayLines(spots, 'all')).toEqual([])
  })

  it('returns one line connecting spots of the day in time order', () => {
    const lines = computeDayLines(spots, 'd1')
    expect(lines).toHaveLength(1)
    expect(lines[0].coordinates).toEqual([
      [-9.1, 38.7],   // s1 first (09:00)
      [-9.11, 38.71], // s2 second (12:00)
    ])
  })

  it('returns no line if the day has less than 2 spots', () => {
    expect(computeDayLines(spots, 'd2')).toEqual([])
  })

  it('orders spots by time ascending, nulls last', () => {
    const mixed: MapSpot[] = [
      { id: 'x', name: 'X', lat: 1, lng: 1, category: 'food', day_id: 'd', time: null },
      { id: 'y', name: 'Y', lat: 2, lng: 2, category: 'food', day_id: 'd', time: '08:00' },
      { id: 'z', name: 'Z', lat: 3, lng: 3, category: 'food', day_id: 'd', time: '15:00' },
    ]
    const lines = computeDayLines(mixed, 'd')
    expect(lines[0].coordinates).toEqual([
      [2, 2], // y first (08:00)
      [3, 3], // z second (15:00)
      [1, 1], // x last (null time)
    ])
  })
})
```

- [ ] **Step 5.2: Run tests, vérifier qu'ils échouent**

```bash
npm test -- --run lib/map/spotFilters.test.ts
```

Expected: FAIL avec "Cannot find module './spotFilters'".

- [ ] **Step 5.3: Implémenter le module**

Create `lib/map/spotFilters.ts`:

```typescript
export interface MapSpot {
  id: string
  name: string
  lat: number
  lng: number
  category: string
  day_id: string | null
  time: string | null // 'HH:MM' or 'HH:MM:SS'
}

export interface DayLine {
  day_id: string
  coordinates: Array<[number, number]> // [lng, lat] pairs, GeoJSON convention
}

export type SelectedDayId = 'all' | string

export function filterVisibleSpots(
  spots: MapSpot[],
  selectedDayId: SelectedDayId,
): MapSpot[] {
  if (selectedDayId === 'all') return spots
  return spots.filter((s) => s.day_id === selectedDayId)
}

function compareTime(a: string | null, b: string | null): number {
  if (a === null && b === null) return 0
  if (a === null) return 1  // null last
  if (b === null) return -1
  return a.localeCompare(b)
}

export function computeDayLines(
  spots: MapSpot[],
  selectedDayId: SelectedDayId,
): DayLine[] {
  if (selectedDayId === 'all') return []
  const daySpots = spots
    .filter((s) => s.day_id === selectedDayId)
    .sort((a, b) => compareTime(a.time, b.time))
  if (daySpots.length < 2) return []
  return [
    {
      day_id: selectedDayId,
      coordinates: daySpots.map((s) => [s.lng, s.lat]),
    },
  ]
}
```

- [ ] **Step 5.4: Run tests, vérifier qu'ils passent**

```bash
npm test -- --run lib/map/spotFilters.test.ts
```

Expected: 9 tests PASS.

- [ ] **Step 5.5: Commit**

```bash
git add lib/map/spotFilters.ts lib/map/spotFilters.test.ts
git commit -m "feat(map): filtres visibleSpots + computeDayLines (TDD)"
```

---

### Task 6: Hook `useTripMapData`

**Files:**
- Create: `lib/map/useTripMapData.ts`
- Test: `lib/map/useTripMapData.test.tsx` (test léger : on s'appuie sur Dexie réel en mémoire via fake-indexeddb)
- Modify: `package.json` (ajouter `fake-indexeddb` en dev pour les tests Dexie)

- [ ] **Step 6.1: Installer fake-indexeddb pour tester Dexie en jsdom**

```bash
npm install --save-dev fake-indexeddb
```

- [ ] **Step 6.2: Créer le setup étendu**

Modifier `vitest.setup.ts` :

```typescript
import '@testing-library/jest-dom/vitest'
import 'fake-indexeddb/auto'
```

- [ ] **Step 6.3: Écrire les tests du hook**

Create `lib/map/useTripMapData.test.tsx`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { renderHook, waitFor } from '@testing-library/react'
import { db } from '@/lib/db'
import { useTripMapData } from './useTripMapData'

const TRIP_ID = 'trip-1'

beforeEach(async () => {
  await db.spots.clear()
  await db.days.clear()
})

describe('useTripMapData', () => {
  it('returns empty arrays when no data', async () => {
    const { result } = renderHook(() => useTripMapData(TRIP_ID))
    await waitFor(() => {
      expect(result.current.days).toEqual([])
      expect(result.current.spots).toEqual([])
    })
  })

  it('returns spots and days of the trip, filtering out other trips', async () => {
    await db.days.bulkAdd([
      { id: 'd1', trip_id: TRIP_ID, day_number: 1, date: '2026-01-01' } as any,
      { id: 'd2', trip_id: 'other', day_number: 1, date: '2026-01-01' } as any,
    ])
    await db.spots.bulkAdd([
      { id: 's1', trip_id: TRIP_ID, name: 'A', lat: 1, lng: 1, category: 'food', day_id: 'd1' } as any,
      { id: 's2', trip_id: 'other', name: 'B', lat: 2, lng: 2, category: 'food', day_id: 'd2' } as any,
    ])
    const { result } = renderHook(() => useTripMapData(TRIP_ID))
    await waitFor(() => {
      expect(result.current.days.map((d) => d.id)).toEqual(['d1'])
      expect(result.current.spots.map((s) => s.id)).toEqual(['s1'])
    })
  })

  it('filters out spots without lat/lng', async () => {
    await db.spots.bulkAdd([
      { id: 's1', trip_id: TRIP_ID, name: 'A', lat: null, lng: null, category: 'food', day_id: null } as any,
      { id: 's2', trip_id: TRIP_ID, name: 'B', lat: 1, lng: 1, category: 'food', day_id: null } as any,
    ])
    const { result } = renderHook(() => useTripMapData(TRIP_ID))
    await waitFor(() => {
      expect(result.current.spots.map((s) => s.id)).toEqual(['s2'])
    })
  })

  it('orders days by day_number ascending', async () => {
    await db.days.bulkAdd([
      { id: 'd3', trip_id: TRIP_ID, day_number: 3, date: '2026-01-03' } as any,
      { id: 'd1', trip_id: TRIP_ID, day_number: 1, date: '2026-01-01' } as any,
      { id: 'd2', trip_id: TRIP_ID, day_number: 2, date: '2026-01-02' } as any,
    ])
    const { result } = renderHook(() => useTripMapData(TRIP_ID))
    await waitFor(() => {
      expect(result.current.days.map((d) => d.day_number)).toEqual([1, 2, 3])
    })
  })
})
```

- [ ] **Step 6.4: Run tests, vérifier qu'ils échouent**

```bash
npm test -- --run lib/map/useTripMapData.test.tsx
```

Expected: FAIL avec "Cannot find module './useTripMapData'".

- [ ] **Step 6.5: Implémenter le hook**

Create `lib/map/useTripMapData.ts`:

```typescript
'use client'

import { useLiveQuery } from 'dexie-react-hooks'
import { db } from '@/lib/db'
import type { LocalDay, LocalSpot } from '@/lib/db/schema'
import type { MapSpot } from './spotFilters'

export interface UseTripMapData {
  days: LocalDay[]
  spots: MapSpot[]
  loading: boolean
}

export function useTripMapData(tripId: string): UseTripMapData {
  const days = useLiveQuery(
    async () => {
      const all = await db.days.where({ trip_id: tripId }).toArray()
      return all.sort((a, b) => (a.day_number ?? 0) - (b.day_number ?? 0))
    },
    [tripId],
  )
  const spotsRaw = useLiveQuery(
    () => db.spots.where({ trip_id: tripId }).toArray(),
    [tripId],
  )
  const spots: MapSpot[] = (spotsRaw ?? [])
    .filter((s): s is LocalSpot & { lat: number; lng: number } =>
      s.lat != null && s.lng != null,
    )
    .map((s) => ({
      id: s.id,
      name: s.name,
      lat: Number(s.lat),
      lng: Number(s.lng),
      category: s.category,
      day_id: (s as any).day_id ?? null,
      time: null, // spots n'ont pas de time, on utilisera l'ordre d'insertion (cf. computeDayLines fallback)
    }))
  return {
    days: days ?? [],
    spots,
    loading: days === undefined || spotsRaw === undefined,
  }
}
```

- [ ] **Step 6.6: Run tests, vérifier qu'ils passent**

```bash
npm test -- --run lib/map/useTripMapData.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 6.7: Commit**

```bash
git add lib/map/useTripMapData.ts lib/map/useTripMapData.test.tsx vitest.setup.ts package.json package-lock.json
git commit -m "feat(map): hook useTripMapData (days + spots filtrés) + tests fake-idb"
```

---

### Task 7: Composant `MapDayDock`

**Files:**
- Create: `components/map/MapDayDock.tsx`
- Test: `components/map/MapDayDock.test.tsx`

- [ ] **Step 7.1: Écrire les tests**

Create `components/map/MapDayDock.test.tsx`:

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MapDayDock } from './MapDayDock'

const days = [
  { id: 'd1', day_number: 1 },
  { id: 'd2', day_number: 2 },
  { id: 'd3', day_number: 3 },
]

describe('MapDayDock', () => {
  it('renders one "Tous" pill + one pill per day', () => {
    render(<MapDayDock days={days} selectedDayId="all" onSelect={() => {}} />)
    expect(screen.getByRole('button', { name: /tous/i })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'J1' })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'J2' })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: 'J3' })).toBeInTheDocument()
  })

  it('marks the selected pill with aria-pressed', () => {
    render(<MapDayDock days={days} selectedDayId="d2" onSelect={() => {}} />)
    expect(screen.getByRole('button', { name: 'J2' })).toHaveAttribute('aria-pressed', 'true')
    expect(screen.getByRole('button', { name: /tous/i })).toHaveAttribute('aria-pressed', 'false')
  })

  it('calls onSelect with the day id when a pill is clicked', async () => {
    const onSelect = vi.fn()
    render(<MapDayDock days={days} selectedDayId="all" onSelect={onSelect} />)
    await userEvent.click(screen.getByRole('button', { name: 'J2' }))
    expect(onSelect).toHaveBeenCalledWith('d2')
  })

  it('calls onSelect with "all" when "Tous" is clicked', async () => {
    const onSelect = vi.fn()
    render(<MapDayDock days={days} selectedDayId="d1" onSelect={onSelect} />)
    await userEvent.click(screen.getByRole('button', { name: /tous/i }))
    expect(onSelect).toHaveBeenCalledWith('all')
  })
})
```

- [ ] **Step 7.2: Run tests, vérifier qu'ils échouent**

```bash
npm test -- --run components/map/MapDayDock.test.tsx
```

Expected: FAIL avec "Cannot find module './MapDayDock'".

- [ ] **Step 7.3: Implémenter le composant**

Create `components/map/MapDayDock.tsx`:

```typescript
'use client'

import { getDayColor } from '@/lib/map/dayColors'
import type { SelectedDayId } from '@/lib/map/spotFilters'

interface Day {
  id: string
  day_number: number | null
}

interface Props {
  days: Day[]
  selectedDayId: SelectedDayId
  onSelect: (id: SelectedDayId) => void
}

export function MapDayDock({ days, selectedDayId, onSelect }: Props) {
  return (
    <div
      className="pointer-events-auto inline-flex items-center gap-1.5 px-2 py-1.5 rounded-pill bg-black/70 backdrop-blur-md shadow-lg"
      role="toolbar"
      aria-label="Filtre par jour"
    >
      <button
        type="button"
        onClick={() => onSelect('all')}
        aria-pressed={selectedDayId === 'all'}
        className={`px-3 py-1 rounded-pill text-[11px] font-medium transition ${
          selectedDayId === 'all'
            ? 'bg-white text-black'
            : 'text-white/80 hover:text-white'
        }`}
      >
        Tous
      </button>
      {days.map((d, idx) => {
        const active = selectedDayId === d.id
        const color = getDayColor(idx)
        return (
          <button
            key={d.id}
            type="button"
            onClick={() => onSelect(d.id)}
            aria-pressed={active}
            className={`w-7 h-7 rounded-full text-[11px] font-bold transition flex items-center justify-center ${
              active
                ? 'text-black ring-2 ring-white scale-110'
                : 'text-white/90 hover:scale-105'
            }`}
            style={{ background: active ? '#fff' : color }}
          >
            J{d.day_number ?? idx + 1}
          </button>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 7.4: Run tests, vérifier qu'ils passent**

```bash
npm test -- --run components/map/MapDayDock.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 7.5: Commit**

```bash
git add components/map/MapDayDock.tsx components/map/MapDayDock.test.tsx
git commit -m "feat(map): composant MapDayDock (pastilles filtre jour)"
```

---

### Task 8: Refonte `MapView` (Markers colorés par jour + lignes)

**Files:**
- Modify: `components/map/MapView.tsx`

**Note** : la signature change. On ajoute `days`, `selectedDayId`, `lines`, `onSpotClick`. Anciens usages dans `app/trips/[tripId]/map/page.tsx` seront mis à jour Task 12.

- [ ] **Step 8.1: Refondre `MapView.tsx`**

Replace contents of `components/map/MapView.tsx`:

```typescript
'use client'

import 'mapbox-gl/dist/mapbox-gl.css'
import { useMemo, useRef, useEffect } from 'react'
import { Map, Marker, NavigationControl, Source, Layer, type MapRef } from 'react-map-gl'
import { getDayColor } from '@/lib/map/dayColors'
import type { MapSpot, DayLine, SelectedDayId } from '@/lib/map/spotFilters'

interface Day {
  id: string
  day_number: number | null
}

interface Props {
  spots: MapSpot[]
  days: Day[]
  lines: DayLine[]
  selectedSpotId: string | null
  selectedDayId: SelectedDayId
  onSpotClick: (spotId: string) => void
}

const CATEGORY_ICONS: Record<string, string> = {
  food: '🍴',
  culture: '🏛',
  nightlife: '🌙',
  nature: '🌲',
  accommodation: '🏠',
  activity: '⚡',
  sport: '🛹',
}

export function MapView({
  spots,
  days,
  lines,
  selectedSpotId,
  selectedDayId,
  onSpotClick,
}: Props) {
  const mapRef = useRef<MapRef | null>(null)

  // Map day_id → index for color lookup
  const dayIndex = useMemo(() => {
    const map = new Map<string, number>()
    days.forEach((d, i) => map.set(d.id, i))
    return map
  }, [days])

  // fitBounds when spots change
  useEffect(() => {
    if (!mapRef.current || spots.length === 0) return
    const lats = spots.map((s) => s.lat)
    const lngs = spots.map((s) => s.lng)
    const sw: [number, number] = [Math.min(...lngs), Math.min(...lats)]
    const ne: [number, number] = [Math.max(...lngs), Math.max(...lats)]
    mapRef.current.fitBounds([sw, ne], { padding: 50, duration: 800, maxZoom: 15 })
  }, [spots])

  // GeoJSON for lines
  const lineGeoJSON = useMemo(
    () => ({
      type: 'FeatureCollection' as const,
      features: lines.map((line) => {
        const idx = dayIndex.get(line.day_id) ?? 0
        return {
          type: 'Feature' as const,
          properties: { color: getDayColor(idx) },
          geometry: {
            type: 'LineString' as const,
            coordinates: line.coordinates,
          },
        }
      }),
    }),
    [lines, dayIndex],
  )

  const center = spots[0] ?? { lat: 38.722, lng: -9.139 }

  return (
    <Map
      ref={mapRef}
      mapboxAccessToken={process.env.NEXT_PUBLIC_MAPBOX_TOKEN}
      initialViewState={{
        latitude: center.lat,
        longitude: center.lng,
        zoom: 11,
      }}
      mapStyle="mapbox://styles/mapbox/light-v11"
      style={{ width: '100%', height: '100%' }}
    >
      <NavigationControl position="top-right" />

      {lines.length > 0 && (
        <Source id="day-lines" type="geojson" data={lineGeoJSON}>
          <Layer
            id="day-lines-layer"
            type="line"
            paint={{
              'line-color': ['get', 'color'],
              'line-width': 3,
              'line-opacity': 0.6,
            }}
            layout={{ 'line-cap': 'round', 'line-join': 'round' }}
          />
        </Source>
      )}

      {spots.map((spot) => {
        const idx = spot.day_id ? dayIndex.get(spot.day_id) ?? 0 : 0
        const color = spot.day_id ? getDayColor(idx) : '#9ca3af' // gray-400 if orphan
        const active = spot.id === selectedSpotId
        const icon = CATEGORY_ICONS[spot.category] ?? '•'
        return (
          <Marker
            key={spot.id}
            latitude={spot.lat}
            longitude={spot.lng}
            onClick={(e) => {
              e.originalEvent.stopPropagation()
              onSpotClick(spot.id)
            }}
          >
            <button
              type="button"
              aria-label={`Spot ${spot.name}`}
              className={`flex items-center justify-center rounded-full border-2 border-white shadow-md transition-transform ${
                active ? 'w-9 h-9 ring-2 ring-white' : 'w-7 h-7 hover:scale-110'
              }`}
              style={{
                background: color,
                transform: active ? 'scale(1.3)' : undefined,
              }}
            >
              <span className="text-[11px] leading-none">{icon}</span>
            </button>
          </Marker>
        )
      })}
    </Map>
  )
}
```

- [ ] **Step 8.2: Vérifier la compilation TypeScript**

```bash
npx tsc --noEmit
```

Expected: 0 erreur dans `components/map/MapView.tsx`. Si erreurs dans `app/trips/[tripId]/map/page.tsx` à cause du changement de signature — c'est attendu, on fix Task 12.

- [ ] **Step 8.3: Commit (note : la page consommatrice est encore cassée à ce stade)**

```bash
git add components/map/MapView.tsx
git commit -m "refactor(map): MapView accepte days/lines/onSpotClick, pins colorés par jour"
```

---

### Task 9: Composant `MapSpotDetailSheet`

**Files:**
- Create: `components/map/MapSpotDetailSheet.tsx`
- Test: `components/map/MapSpotDetailSheet.test.tsx`

- [ ] **Step 9.1: Écrire les tests**

Create `components/map/MapSpotDetailSheet.test.tsx`:

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MapSpotDetailSheet } from './MapSpotDetailSheet'

const spot = {
  id: 's1',
  name: 'Time Out Market',
  category: 'food',
  description: 'Food hall iconique de Lisbonne',
  lat: 38.7,
  lng: -9.1,
  day_id: 'd1',
  time: null,
}

describe('MapSpotDetailSheet', () => {
  it('renders nothing when spot is null', () => {
    const { container } = render(
      <MapSpotDetailSheet spot={null} onClose={() => {}} accentColor="#000" />,
    )
    expect(container).toBeEmptyDOMElement()
  })

  it('renders the spot name and category when spot is provided', () => {
    render(
      <MapSpotDetailSheet spot={spot as any} onClose={() => {}} accentColor="#000" />,
    )
    expect(screen.getByText('Time Out Market')).toBeInTheDocument()
    expect(screen.getByText(/food/i)).toBeInTheDocument()
  })

  it('renders the description when present', () => {
    render(
      <MapSpotDetailSheet spot={spot as any} onClose={() => {}} accentColor="#000" />,
    )
    expect(screen.getByText(/food hall iconique/i)).toBeInTheDocument()
  })

  it('calls onClose when close button is clicked', async () => {
    const onClose = vi.fn()
    render(
      <MapSpotDetailSheet spot={spot as any} onClose={onClose} accentColor="#000" />,
    )
    await userEvent.click(screen.getByRole('button', { name: /fermer/i }))
    expect(onClose).toHaveBeenCalled()
  })
})
```

- [ ] **Step 9.2: Run tests, vérifier qu'ils échouent**

```bash
npm test -- --run components/map/MapSpotDetailSheet.test.tsx
```

Expected: FAIL avec "Cannot find module './MapSpotDetailSheet'".

- [ ] **Step 9.3: Implémenter le composant**

Create `components/map/MapSpotDetailSheet.tsx`:

```typescript
'use client'

import { Drawer } from 'vaul'
import type { LocalSpot } from '@/lib/db/schema'

interface Props {
  spot: (LocalSpot & { lat: number; lng: number }) | null
  onClose: () => void
  accentColor: string
}

export function MapSpotDetailSheet({ spot, onClose, accentColor }: Props) {
  if (!spot) return null
  return (
    <Drawer.Root open={true} onOpenChange={(open) => !open && onClose()} dismissible>
      <Drawer.Portal>
        <Drawer.Overlay className="fixed inset-0 bg-black/30 z-40" />
        <Drawer.Content className="fixed bottom-0 left-0 right-0 z-50 max-h-[80vh] bg-white dark:bg-paper-dark-deep rounded-t-[20px] outline-none shadow-2xl">
          <div className="w-[38px] h-1 bg-hairline-strong rounded-full mx-auto mt-3 mb-3.5" />
          <div className="px-5 pb-6">
            <div className="flex items-start justify-between gap-3">
              <div className="min-w-0">
                <div
                  className="mk-mono text-[10px] uppercase tracking-wider mb-1"
                  style={{ color: accentColor }}
                >
                  {spot.category}
                </div>
                <Drawer.Title asChild>
                  <h2 className="font-display font-bold text-xl truncate">{spot.name}</h2>
                </Drawer.Title>
              </div>
              <button
                type="button"
                onClick={onClose}
                aria-label="Fermer"
                className="w-9 h-9 rounded-full bg-hairline/40 flex items-center justify-center text-lg shrink-0"
              >
                ×
              </button>
            </div>
            {spot.description && (
              <p className="mt-4 text-sm text-ink-mute dark:text-ink-mute-dark leading-relaxed">
                {spot.description}
              </p>
            )}
            <div className="mt-5 flex gap-2 text-[11px] text-ink-mute dark:text-ink-mute-dark mk-mono">
              <span>{spot.lat.toFixed(4)}</span>
              <span>·</span>
              <span>{spot.lng.toFixed(4)}</span>
            </div>
          </div>
        </Drawer.Content>
      </Drawer.Portal>
    </Drawer.Root>
  )
}
```

- [ ] **Step 9.4: Run tests, vérifier qu'ils passent**

```bash
npm test -- --run components/map/MapSpotDetailSheet.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 9.5: Commit**

```bash
git add components/map/MapSpotDetailSheet.tsx components/map/MapSpotDetailSheet.test.tsx
git commit -m "feat(map): composant MapSpotDetailSheet (drawer Vaul détail spot)"
```

---

### Task 10: Refonte `MapSpotSheet` (snap points + liste filtrée)

**Files:**
- Modify: `components/map/MapSheet.tsx` → renommer en `MapSpotSheet.tsx` (nouveau scope)
- Test: `components/map/MapSpotSheet.test.tsx`

**Note** : on supprime `MapSheet.tsx` ancien (les FILTERS inutilisés). On crée `MapSpotSheet.tsx` neuf.

- [ ] **Step 10.1: Supprimer l'ancien fichier**

```bash
git rm components/map/MapSheet.tsx
```

- [ ] **Step 10.2: Écrire les tests du nouveau composant**

Create `components/map/MapSpotSheet.test.tsx`:

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MapSpotSheet } from './MapSpotSheet'

const spots = [
  { id: 's1', name: 'Castelo', category: 'culture', lat: 38.71, lng: -9.13 },
  { id: 's2', name: 'Time Out', category: 'food', lat: 38.71, lng: -9.14 },
]

describe('MapSpotSheet', () => {
  it('displays the spot count and label', () => {
    render(
      <MapSpotSheet
        spots={spots as any}
        label="Tous les spots"
        onSpotClick={() => {}}
      />,
    )
    expect(screen.getByText(/tous les spots/i)).toBeInTheDocument()
    expect(screen.getByText(/2 spots/i)).toBeInTheDocument()
  })

  it('renders one row per spot', () => {
    render(
      <MapSpotSheet
        spots={spots as any}
        label="Tous"
        onSpotClick={() => {}}
      />,
    )
    expect(screen.getByText('Castelo')).toBeInTheDocument()
    expect(screen.getByText('Time Out')).toBeInTheDocument()
  })

  it('calls onSpotClick with the spot id when a row is clicked', async () => {
    const onSpotClick = vi.fn()
    render(
      <MapSpotSheet
        spots={spots as any}
        label="Tous"
        onSpotClick={onSpotClick}
      />,
    )
    await userEvent.click(screen.getByText('Castelo'))
    expect(onSpotClick).toHaveBeenCalledWith('s1')
  })

  it('renders empty state when spots is empty', () => {
    render(
      <MapSpotSheet spots={[]} label="Jour 2" onSpotClick={() => {}} />,
    )
    expect(screen.getByText(/aucun spot/i)).toBeInTheDocument()
  })
})
```

- [ ] **Step 10.3: Run tests, vérifier qu'ils échouent**

```bash
npm test -- --run components/map/MapSpotSheet.test.tsx
```

Expected: FAIL avec "Cannot find module './MapSpotSheet'".

- [ ] **Step 10.4: Implémenter le composant**

Create `components/map/MapSpotSheet.tsx`:

```typescript
'use client'

import { Drawer } from 'vaul'
import { Eyebrow } from '@/components/design/Eyebrow'
import type { MapSpot } from '@/lib/map/spotFilters'

interface Props {
  spots: MapSpot[]
  label: string // ex: "Tous les spots" ou "Jour 2"
  onSpotClick: (spotId: string) => void
}

const CATEGORY_ICONS: Record<string, string> = {
  food: '🍴',
  culture: '🏛',
  nightlife: '🌙',
  nature: '🌲',
  accommodation: '🏠',
  activity: '⚡',
  sport: '🛹',
}

// Vaul snap points : 18% peek / 50% mid / 95% full
const SNAP_POINTS = ['180px', 0.5, 0.95]

export function MapSpotSheet({ spots, label, onSpotClick }: Props) {
  return (
    <Drawer.Root
      open
      dismissible={false}
      modal={false}
      snapPoints={SNAP_POINTS}
      defaultSnapPoint="180px"
    >
      <Drawer.Portal>
        <Drawer.Content className="fixed bottom-0 left-0 right-0 z-30 bg-white dark:bg-paper-dark-deep rounded-t-[16px] shadow-sheet outline-none">
          <div className="w-[38px] h-1 bg-hairline-strong rounded-full mx-auto mt-3 mb-3.5" />
          <div className="px-5 flex items-end justify-between">
            <div>
              <Eyebrow className="text-ink-mute dark:text-ink-mute-dark">{label}</Eyebrow>
              <div className="font-display font-bold text-lg mt-0.5">
                {spots.length} spot{spots.length > 1 ? 's' : ''}
              </div>
            </div>
          </div>
          {spots.length === 0 ? (
            <div className="px-5 mt-6 pb-8 text-sm text-ink-mute dark:text-ink-mute-dark">
              Aucun spot pour ce filtre.
            </div>
          ) : (
            <ul className="mt-3 px-5 pb-6 overflow-y-auto max-h-[calc(95vh-80px)] mk-noscroll">
              {spots.map((s, i) => (
                <li
                  key={s.id}
                  className={`py-3 ${i ? 'border-t border-hairline dark:border-hairline-dark' : ''}`}
                >
                  <button
                    type="button"
                    onClick={() => onSpotClick(s.id)}
                    className="w-full flex items-center gap-3 text-left"
                  >
                    <div className="w-9 h-9 rounded-xs bg-hairline/40 flex items-center justify-center text-base">
                      {CATEGORY_ICONS[s.category] ?? '•'}
                    </div>
                    <div className="flex-1 min-w-0">
                      <div className="text-sm font-medium truncate">{s.name}</div>
                      <div className="mk-mono text-[10px] text-ink-mute dark:text-ink-mute-dark mt-0.5">
                        {s.category.toUpperCase()}
                      </div>
                    </div>
                  </button>
                </li>
              ))}
            </ul>
          )}
        </Drawer.Content>
      </Drawer.Portal>
    </Drawer.Root>
  )
}
```

- [ ] **Step 10.5: Run tests, vérifier qu'ils passent**

```bash
npm test -- --run components/map/MapSpotSheet.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 10.6: Commit**

```bash
git add components/map/MapSpotSheet.tsx components/map/MapSpotSheet.test.tsx
git commit -m "feat(map): MapSpotSheet (Vaul 3 snap points + liste filtrée cliquable)"
```

---

### Task 11: Composant orchestrateur `MapShell`

**Files:**
- Create: `components/map/MapShell.tsx`

Pas de test unit pour l'orchestrateur — il sera couvert par les E2E (Task 13). C'est un assemblage simple de composants déjà testés.

- [ ] **Step 11.1: Créer le composant**

Create `components/map/MapShell.tsx`:

```typescript
'use client'

import { useState, useMemo } from 'react'
import dynamic from 'next/dynamic'
import { useLiveQuery } from 'dexie-react-hooks'
import { db } from '@/lib/db'
import { accentFor } from '@/lib/design/accent'
import { useTripMapData } from '@/lib/map/useTripMapData'
import { filterVisibleSpots, computeDayLines, type SelectedDayId } from '@/lib/map/spotFilters'
import { MapDayDock } from './MapDayDock'
import { MapSpotSheet } from './MapSpotSheet'
import { MapSpotDetailSheet } from './MapSpotDetailSheet'

const MapView = dynamic(
  () => import('./MapView').then((m) => m.MapView),
  { ssr: false },
)

interface Props {
  tripId: string
}

export function MapShell({ tripId }: Props) {
  const trip = useLiveQuery(() => db.trips.get(tripId), [tripId])
  const { days, spots } = useTripMapData(tripId)
  const [selectedDayId, setSelectedDayId] = useState<SelectedDayId>('all')
  const [selectedSpotId, setSelectedSpotId] = useState<string | null>(null)

  const visibleSpots = useMemo(
    () => filterVisibleSpots(spots, selectedDayId),
    [spots, selectedDayId],
  )
  const lines = useMemo(
    () => computeDayLines(spots, selectedDayId),
    [spots, selectedDayId],
  )

  const selectedSpot = useMemo(
    () => spots.find((s) => s.id === selectedSpotId) ?? null,
    [spots, selectedSpotId],
  )
  // Re-fetch full LocalSpot for detail (includes description, image_url)
  const selectedSpotFull = useLiveQuery(
    () => (selectedSpotId ? db.spots.get(selectedSpotId) : null),
    [selectedSpotId],
  )

  if (!trip) return null
  const accent = accentFor(trip.trip_type)

  const dayLabel = (() => {
    if (selectedDayId === 'all') return 'Tous les spots'
    const d = days.find((dd) => dd.id === selectedDayId)
    return d ? `Jour ${d.day_number ?? '?'}` : 'Jour'
  })()

  return (
    <div className="fixed inset-0 z-10" data-testid="map-shell">
      {/* Map canvas plein écran */}
      <div className="absolute inset-0">
        <MapView
          spots={visibleSpots}
          days={days}
          lines={lines}
          selectedSpotId={selectedSpotId}
          selectedDayId={selectedDayId}
          onSpotClick={setSelectedSpotId}
        />
      </div>

      {/* Day dock flottant */}
      {days.length > 0 && (
        <div className="absolute top-4 left-1/2 -translate-x-1/2 z-20 pointer-events-none">
          <MapDayDock
            days={days}
            selectedDayId={selectedDayId}
            onSelect={(id) => {
              setSelectedDayId(id)
              setSelectedSpotId(null)
            }}
          />
        </div>
      )}

      {/* Bottom sheet liste spots */}
      <MapSpotSheet
        spots={visibleSpots}
        label={dayLabel}
        onSpotClick={setSelectedSpotId}
      />

      {/* Sheet détail spot (overlay) */}
      <MapSpotDetailSheet
        spot={selectedSpotFull && selectedSpotFull.lat != null && selectedSpotFull.lng != null
          ? (selectedSpotFull as any)
          : null}
        onClose={() => setSelectedSpotId(null)}
        accentColor={accent.base}
      />
    </div>
  )
}
```

- [ ] **Step 11.2: Vérifier la compilation TypeScript**

```bash
npx tsc --noEmit
```

Expected: 0 erreur sur le nouveau fichier. Erreurs résiduelles dans `app/trips/[tripId]/map/page.tsx` à cause de l'ancien import de `MapSheet` supprimé — fix en Task 12.

- [ ] **Step 11.3: Commit**

```bash
git add components/map/MapShell.tsx
git commit -m "feat(map): orchestrateur MapShell (state + composition)"
```

---

### Task 12: Remplacer `app/trips/[tripId]/map/page.tsx` + feature flag

**Files:**
- Modify: `app/trips/[tripId]/map/page.tsx`

- [ ] **Step 12.1: Réécrire la page**

Replace contents of `app/trips/[tripId]/map/page.tsx`:

```typescript
'use client'

import { useParams } from 'next/navigation'
import { MapShell } from '@/components/map/MapShell'

const MAP_SHELL_V2_ENABLED =
  process.env.NEXT_PUBLIC_MAP_SHELL_V2 === 'true' ||
  process.env.NEXT_PUBLIC_MAP_SHELL_V2 === undefined // default ON, set 'false' for rollback

export default function MapPage() {
  const { tripId } = useParams<{ tripId: string }>()
  if (!MAP_SHELL_V2_ENABLED) {
    return (
      <main className="h-screen flex items-center justify-center p-8 text-center">
        <div>
          <h1 className="font-display font-bold text-xl mb-2">Map en maintenance</h1>
          <p className="text-sm text-ink-mute">
            Réactive la carte v2 avec <code>NEXT_PUBLIC_MAP_SHELL_V2=true</code>.
          </p>
        </div>
      </main>
    )
  }
  return <MapShell tripId={tripId} />
}
```

- [ ] **Step 12.2: Vérifier la compilation**

```bash
npx tsc --noEmit
```

Expected: 0 erreur globalement.

- [ ] **Step 12.3: Smoke test manuel**

```bash
npm run dev
```

Ouvrir `http://localhost:3000`, naviguer vers un voyage démo (Lisbonne), cliquer sur la tab Map. Vérifier visuellement :
- Carte plein écran ✅
- Day dock visible en haut ✅
- Bottom sheet visible avec les spots ✅
- Cliquer un pin → sheet détail s'ouvre ✅
- Cliquer une pastille jour → la carte filtre ✅
- Cliquer "Tous" → reset ✅

- [ ] **Step 12.4: Commit**

```bash
git add app/trips/[tripId]/map/page.tsx
git commit -m "feat(map): brancher MapShell sur la page Map + feature flag rollback"
```

---

### Task 13: E2E Playwright (4 scénarios acceptance)

**Files:**
- Create: `e2e/map-shell.spec.ts`

**Pré-requis** : un voyage démo accessible sans auth en local. Vérifier la route `/demo/<tripId>/map` ou `/trips/<tripId>/map` si auth bypass.

- [ ] **Step 13.1: Identifier l'URL démo accessible**

```bash
# Lancer le dev server
npm run dev
```

Ouvrir `http://localhost:3000/demo` (route démo existante) et noter une URL voyage qui ouvre directement. Exemple attendu : `/demo/lisbonne/map`.

Mettre à jour la constante `DEMO_TRIP_MAP_URL` dans le test ci-dessous selon ce qu'on trouve.

- [ ] **Step 13.2: Créer les E2E**

Create `e2e/map-shell.spec.ts`:

```typescript
import { test, expect } from '@playwright/test'

// À ajuster selon la route démo réelle de MK Trip
const DEMO_TRIP_MAP_URL = '/demo/lisbonne/map'

test.describe('Map Shell v2', () => {
  test('Scenario 1: opens with all spots visible and "Tous" selected', async ({ page }) => {
    await page.goto(DEMO_TRIP_MAP_URL)
    await expect(page.getByTestId('map-shell')).toBeVisible()
    // Day dock present
    await expect(page.getByRole('button', { name: /tous/i })).toHaveAttribute('aria-pressed', 'true')
    // Sheet shows count
    await expect(page.getByText(/\d+ spots?/)).toBeVisible({ timeout: 10_000 })
  })

  test('Scenario 2: clicking a day pill filters spots', async ({ page }) => {
    await page.goto(DEMO_TRIP_MAP_URL)
    await page.waitForSelector('[data-testid="map-shell"]')
    // Wait for at least day 1 pill
    const j1 = page.getByRole('button', { name: 'J1' })
    await expect(j1).toBeVisible({ timeout: 10_000 })
    await j1.click()
    await expect(j1).toHaveAttribute('aria-pressed', 'true')
    await expect(page.getByText(/jour 1/i)).toBeVisible()
  })

  test('Scenario 3: clicking a spot in the sheet opens the detail sheet', async ({ page }) => {
    await page.goto(DEMO_TRIP_MAP_URL)
    await page.waitForSelector('[data-testid="map-shell"]')
    // First spot row in the bottom sheet
    const firstSpot = page.locator('ul > li button').first()
    await expect(firstSpot).toBeVisible({ timeout: 10_000 })
    const spotName = await firstSpot.locator('.font-medium').first().textContent()
    await firstSpot.click()
    // Detail sheet has a close button
    await expect(page.getByRole('button', { name: /fermer/i })).toBeVisible()
    if (spotName) {
      await expect(page.getByRole('heading', { name: spotName })).toBeVisible()
    }
  })

  test('Scenario 4: closing the detail sheet returns to the spot list', async ({ page }) => {
    await page.goto(DEMO_TRIP_MAP_URL)
    await page.waitForSelector('[data-testid="map-shell"]')
    const firstSpot = page.locator('ul > li button').first()
    await firstSpot.click()
    const closeBtn = page.getByRole('button', { name: /fermer/i })
    await expect(closeBtn).toBeVisible()
    await closeBtn.click()
    await expect(closeBtn).not.toBeVisible()
    // Sheet list still rendered
    await expect(page.getByText(/\d+ spots?/)).toBeVisible()
  })
})
```

- [ ] **Step 13.3: Lancer les E2E**

```bash
npm run test:e2e -- e2e/map-shell.spec.ts
```

Expected: 4 tests PASS. Si fail à cause de l'URL démo, ajuster `DEMO_TRIP_MAP_URL` et relancer.

- [ ] **Step 13.4: Commit**

```bash
git add e2e/map-shell.spec.ts
git commit -m "test(e2e): 4 scénarios acceptance Map Shell v2"
```

---

### Task 14: Acceptance manuel + preview Vercel

- [ ] **Step 14.1: Pousser la branche**

```bash
git push -u origin feat/map-shell-v2
```

- [ ] **Step 14.2: Attendre/vérifier le déploiement preview Vercel**

Vercel devrait builder automatiquement la branche. Récupérer l'URL preview depuis le dashboard Vercel ou le commentaire GitHub si PR ouverte.

- [ ] **Step 14.3: Checklist acceptance manuelle sur preview**

Ouvrir l'URL preview, voyage démo Lisbonne, sur **mobile (iOS Safari) + desktop Chrome** :

| # | Critère | OK ? |
|---|---|---|
| 1 | Chargement page Map < 2s sur 4G simulée | ☐ |
| 2 | Tous les spots visibles au mount, fitBounds correct | ☐ |
| 3 | Day dock affiche pastilles "Tous · J1 · J2 …" | ☐ |
| 4 | Clic J2 → carte recentre + filtre liste + trace ligne jaune (couleur J2 = amber) | ☐ |
| 5 | Pin coloré selon jour, icône catégorie au centre | ☐ |
| 6 | Clic pin → sheet détail ouvert avec nom + cat | ☐ |
| 7 | Clic item liste → même comportement | ☐ |
| 8 | Sheet draggable fluide entre 3 snap (peek/mid/full) | ☐ |
| 9 | Fermer sheet détail → retour sheet liste sans glitch | ☐ |
| 10 | Aucune régression sur Home / Planning / Budget / Guide | ☐ |
| 11 | Mode démo `/demo` toujours fonctionnel | ☐ |
| 12 | Mode sombre OK (pas de texte invisible) | ☐ |

- [ ] **Step 14.4: Si tout OK, ouvrir une PR vers `main`**

```bash
gh pr create --title "feat(map): refonte écran Map en shell immersif (sous-projet A)" --body "$(cat <<'EOF'
## Summary
- Refonte de la page Map d'un voyage en shell immersif : carte plein écran + day dock + bottom sheet 3 snap + sheet détail spot
- Spec : `docs/superpowers/specs/2026-05-22-map-shell-design.md`
- Plan : `docs/superpowers/plans/2026-05-22-map-shell.md`
- Migration Supabase : ajout `day_id` nullable sur `spots`
- Tests : unit Vitest (palette, filtres, hook, composants) + E2E Playwright (4 scénarios)
- Feature flag : `NEXT_PUBLIC_MAP_SHELL_V2=false` pour rollback

## Test plan
- [x] Tests unitaires (`npm test`) PASS
- [x] Tests E2E (`npm run test:e2e`) PASS
- [x] Acceptance manuelle sur preview Vercel (12 critères)
- [x] Mobile iOS Safari + Desktop Chrome

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 14.5: Mémoire roadmap → marquer A comme livré**

Mettre à jour `MEMORY.md` et `project_roadmap_wanderlog_features.md` pour marquer **A** comme `🟣 livré` avec lien vers la PR. (Action manuelle ou via une session Claude ultérieure.)

---

## Hors-scope confirmé (NE PAS faire dans ce plan)

- Routing réel Mapbox Directions → sous-projet D
- Drag-drop réordonnancement spots → sous-projet C
- Suggestions IA → sous-projet B
- Long-press carte = ajouter spot → futur
- Refonte nav globale (top tabs N2) → potentiel sous-projet ultérieur si validation user OK
- Refonte Home/Planning/Budget/Guide → inchangé
- Optimisation TSP ordre spots → sous-projet D
- Photos auto Google Places → futur

## Risques connus & plans de mitigation

| Risque | Détection | Mitigation |
|---|---|---|
| Sheet Vaul `snapPoints` mal supporté iOS PWA | E2E `mobile` project Playwright | Plan B : `react-spring-bottom-sheet` ou impl maison framer-motion |
| Perf > 50 markers React | Acceptance #1 (charge < 2s) | Refactor vers GeoJSON `circle` + `symbol` layers Mapbox (sortable en hot-fix) |
| Migration Supabase échoue en prod | Vercel build / déploiement | Migration est ALTER ADD COLUMN nullable → safe, pas de lock long |
| Régression sur `/demo` | Acceptance #11 | Tester explicitement avant merge |
| Couleur jour invisible sur fond clair Mapbox | Acceptance #4, #5 (visuel) | Palette choisie déjà saturée, ajuster opacité bordure pin si besoin |
