# Chat intégré au trip — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un chat temps réel par trip, offline-first, intégré dans la navigation existante.

**Architecture:** Un chat par trip (pas de channels pour le MVP — extensible plus tard via une colonne). Messages stockés en Supabase avec RLS `is_trip_member`, sync offline via Dexie + sync queue, temps réel via Supabase Realtime (même channel `trip:{id}` existant). UI intégrée comme 6ème tab dans la nav.

**Tech Stack:** Supabase (table + RLS + Realtime), Dexie (offline), Next.js App Router, shadcn/ui, Tailwind, lucide-react.

---

## File Structure

| Action | Fichier | Responsabilité |
|---|---|---|
| Create | `supabase/migrations/20260524000000_chat_messages.sql` | Table `messages` + RLS |
| Modify | `lib/db/schema.ts` | Ajouter `LocalMessage` |
| Modify | `lib/db/index.ts` | Dexie v3 avec table `messages` |
| Modify | `lib/db/hydrate.ts` | Hydrater les messages au chargement du trip |
| Modify | `lib/db/realtime.ts` | Ajouter `messages` au tableau TABLES |
| Modify | `lib/db/mutations.ts` | Ajouter `mutations.message.create/delete` |
| Create | `components/chat/MessageBubble.tsx` | Bulle de message individuelle |
| Create | `components/chat/MessageList.tsx` | Liste scrollable avec auto-scroll |
| Create | `components/chat/ChatInput.tsx` | Champ de saisie + bouton envoi |
| Create | `components/chat/ChatShell.tsx` | Container principal (list + input) |
| Create | `app/trips/[tripId]/chat/page.tsx` | Page route du chat |
| Modify | `app/trips/[tripId]/(nav)/side-rail.tsx` | Ajouter tab Chat |
| Modify | `app/trips/[tripId]/(nav)/bottom-tab.tsx` | Ajouter tab Chat |
| Create | `e2e/chat.spec.ts` | Test E2E du flow chat |

---

### Task 1: Migration Supabase — table `messages` + RLS

**Files:**
- Create: `supabase/migrations/20260524000000_chat_messages.sql`

- [ ] **Step 1: Créer le fichier de migration**

```sql
-- ─── messages ─────────────────────────────────────────────────────────────────
create table public.messages (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  user_id uuid not null references public.profiles(id) on delete cascade,
  body text not null check (char_length(body) > 0 and char_length(body) <= 4000),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index idx_messages_trip_created on public.messages(trip_id, created_at);
create trigger trg_messages_updated_at before update on public.messages
  for each row execute function public.set_updated_at();

-- ─── RLS ──────────────────────────────────────────────────────────────────────
alter table public.messages enable row level security;

create policy "messages_select_member" on public.messages
  for select using (public.is_trip_member(trip_id));

create policy "messages_insert_member" on public.messages
  for insert with check (
    auth.uid() = user_id
    and public.is_trip_member(trip_id)
  );

create policy "messages_delete_self" on public.messages
  for delete using (auth.uid() = user_id);

-- Pas d'UPDATE : un message envoyé n'est pas modifiable (MVP).

-- ─── Realtime : activer la publication pour messages ──────────────────────────
alter publication supabase_realtime add table public.messages;
```

- [ ] **Step 2: Commit**

```bash
git add supabase/migrations/20260524000000_chat_messages.sql
git commit -m "feat(chat): migration table messages + RLS + realtime publication"
```

---

### Task 2: Types TypeScript + Dexie v3

**Files:**
- Modify: `lib/db/schema.ts`
- Modify: `lib/db/index.ts`

- [ ] **Step 1: Ajouter `LocalMessage` dans schema.ts**

Ajouter après la ligne `export type LocalGuideCard = ...` :

```typescript
export type LocalMessage = LocalRow<Tables['messages']['Row']>
```

- [ ] **Step 2: Ajouter la table `messages` dans index.ts**

Ajouter la propriété dans la classe `MKTripDB` (après `guide_cards`) :

```typescript
messages!: EntityTable<LocalMessage, 'id'>
```

Ajouter l'import de `LocalMessage` dans la liste des imports.

Ajouter une version 3 après `.version(2)` :

```typescript
this.version(3).stores({
  messages: 'id, trip_id, [trip_id+created_at]',
})
```

- [ ] **Step 3: Vérifier que l'app démarre sans erreur**

Run: `npm run dev`
Expected: L'app démarre, la console n'affiche pas d'erreur Dexie (le schéma se met à jour automatiquement).

- [ ] **Step 4: Commit**

```bash
git add lib/db/schema.ts lib/db/index.ts
git commit -m "feat(chat): types LocalMessage + Dexie v3 avec table messages"
```

---

### Task 3: Hydrate + Realtime

**Files:**
- Modify: `lib/db/hydrate.ts`
- Modify: `lib/db/realtime.ts`

- [ ] **Step 1: Ajouter `messages` dans hydrateTrip**

Dans `lib/db/hydrate.ts`, ajouter `'messages'` au tableau `tripIdTables` :

```typescript
const tripIdTables = ['days', 'spots', 'expenses', 'checklist_items', 'guide_cards', 'messages'] as const
```

- [ ] **Step 2: Ajouter `messages` dans le realtime**

Dans `lib/db/realtime.ts`, ajouter `'messages'` au tableau `TABLES` :

```typescript
const TABLES = [
  'trips', 'trip_members', 'days', 'activities', 'activity_completions',
  'spots', 'expenses', 'expense_splits', 'checklist_items',
  'checklist_completions', 'guide_cards', 'messages',
] as const
```

- [ ] **Step 3: Commit**

```bash
git add lib/db/hydrate.ts lib/db/realtime.ts
git commit -m "feat(chat): hydrate + realtime pour messages"
```

---

### Task 4: Mutations

**Files:**
- Modify: `lib/db/mutations.ts`

- [ ] **Step 1: Ajouter `mutations.message`**

Ajouter dans l'objet `mutations`, après le bloc `checklist` :

```typescript
message: {
  create: (data: InsertWithTempId<Tables['messages']['Insert']>) =>
    localInsert('messages', data),
  delete: (id: string) => localDelete('messages', id),
},
```

- [ ] **Step 2: Commit**

```bash
git add lib/db/mutations.ts
git commit -m "feat(chat): mutations message.create + message.delete"
```

---

### Task 5: Composant `MessageBubble`

**Files:**
- Create: `components/chat/MessageBubble.tsx`

- [ ] **Step 1: Écrire le test unitaire**

Créer `components/chat/MessageBubble.test.tsx` :

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MessageBubble } from './MessageBubble'

describe('MessageBubble', () => {
  const base = {
    body: 'Salut tout le monde !',
    authorName: 'Paul',
    timestamp: '2026-05-24T14:30:00Z',
    isMine: false,
  }

  it('affiche le body du message', () => {
    render(<MessageBubble {...base} />)
    expect(screen.getByText('Salut tout le monde !')).toBeDefined()
  })

  it('affiche le nom de l'auteur pour les messages des autres', () => {
    render(<MessageBubble {...base} />)
    expect(screen.getByText('Paul')).toBeDefined()
  })

  it('n'affiche pas le nom de l'auteur pour ses propres messages', () => {
    render(<MessageBubble {...base} isMine />)
    expect(screen.queryByText('Paul')).toBeNull()
  })

  it('aligne à droite pour isMine', () => {
    const { container } = render(<MessageBubble {...base} isMine />)
    const wrapper = container.firstElementChild as HTMLElement
    expect(wrapper.className).toContain('justify-end')
  })
})
```

- [ ] **Step 2: Vérifier que les tests échouent**

Run: `npx vitest run components/chat/MessageBubble.test.tsx`
Expected: FAIL — module non trouvé.

- [ ] **Step 3: Implémenter MessageBubble**

Créer `components/chat/MessageBubble.tsx` :

```tsx
'use client'

interface MessageBubbleProps {
  body: string
  authorName: string
  timestamp: string
  isMine: boolean
}

export function MessageBubble({ body, authorName, timestamp, isMine }: MessageBubbleProps) {
  const time = new Date(timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })

  return (
    <div className={`flex ${isMine ? 'justify-end' : 'justify-start'} mb-2`}>
      <div
        className={`max-w-[75%] rounded-2xl px-3.5 py-2 ${
          isMine
            ? 'bg-ink dark:bg-ink-dark text-paper dark:text-paper-dark rounded-br-sm'
            : 'bg-sand/50 dark:bg-sand-dark/50 rounded-bl-sm'
        }`}
      >
        {!isMine && (
          <p className="text-xs font-semibold text-ink-mute dark:text-ink-mute-dark mb-0.5">
            {authorName}
          </p>
        )}
        <p className="text-sm whitespace-pre-wrap break-words">{body}</p>
        <p
          className={`text-[10px] mt-1 ${
            isMine ? 'text-paper/60 dark:text-paper-dark/60' : 'text-ink-mute dark:text-ink-mute-dark'
          }`}
        >
          {time}
        </p>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run components/chat/MessageBubble.test.tsx`
Expected: 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add components/chat/MessageBubble.tsx components/chat/MessageBubble.test.tsx
git commit -m "feat(chat): composant MessageBubble avec tests"
```

---

### Task 6: Composant `MessageList`

**Files:**
- Create: `components/chat/MessageList.tsx`

- [ ] **Step 1: Implémenter MessageList**

```tsx
'use client'

import { useEffect, useRef } from 'react'
import { useLiveQuery } from 'dexie-react-hooks'
import { db } from '@/lib/db'
import { MessageBubble } from './MessageBubble'

interface MessageListProps {
  tripId: string
  currentUserId: string
}

export function MessageList({ tripId, currentUserId }: MessageListProps) {
  const bottomRef = useRef<HTMLDivElement>(null)

  const messages = useLiveQuery(
    () => db.messages.where('trip_id').equals(tripId).sortBy('created_at'),
    [tripId],
  ) ?? []

  const profiles = useLiveQuery(() => db.profiles.toArray(), []) ?? []
  const profileMap = new Map(profiles.map((p) => [p.id, p.display_name]))

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' })
  }, [messages.length])

  if (messages.length === 0) {
    return (
      <div className="flex-1 flex items-center justify-center text-ink-mute dark:text-ink-mute-dark">
        <p className="text-sm">Aucun message. Lancez la conversation !</p>
      </div>
    )
  }

  return (
    <div className="flex-1 overflow-y-auto px-4 py-3">
      {messages.map((msg) => (
        <MessageBubble
          key={msg.id}
          body={msg.body}
          authorName={profileMap.get(msg.user_id) ?? 'Inconnu'}
          timestamp={msg.created_at}
          isMine={msg.user_id === currentUserId}
        />
      ))}
      <div ref={bottomRef} />
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add components/chat/MessageList.tsx
git commit -m "feat(chat): composant MessageList avec auto-scroll"
```

---

### Task 7: Composant `ChatInput`

**Files:**
- Create: `components/chat/ChatInput.tsx`

- [ ] **Step 1: Implémenter ChatInput**

```tsx
'use client'

import { useState, useCallback, type KeyboardEvent } from 'react'
import { SendHorizonal } from 'lucide-react'
import { mutations } from '@/lib/db/mutations'

interface ChatInputProps {
  tripId: string
  userId: string
}

export function ChatInput({ tripId, userId }: ChatInputProps) {
  const [text, setText] = useState('')

  const send = useCallback(async () => {
    const body = text.trim()
    if (!body) return
    setText('')
    await mutations.message.create({
      trip_id: tripId,
      user_id: userId,
      body,
    })
  }, [text, tripId, userId])

  const handleKeyDown = (e: KeyboardEvent<HTMLTextAreaElement>) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault()
      send()
    }
  }

  return (
    <div className="border-t border-hairline dark:border-hairline-dark px-4 py-3 flex items-end gap-2">
      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        onKeyDown={handleKeyDown}
        placeholder="Message..."
        rows={1}
        className="flex-1 resize-none bg-sand/30 dark:bg-sand-dark/30 rounded-xl px-3.5 py-2.5 text-sm outline-none placeholder:text-ink-mute dark:placeholder:text-ink-mute-dark max-h-32 overflow-y-auto"
      />
      <button
        onClick={send}
        disabled={!text.trim()}
        className="p-2.5 rounded-full bg-ink dark:bg-ink-dark text-paper dark:text-paper-dark disabled:opacity-30 transition-opacity"
      >
        <SendHorizonal className="w-4 h-4" />
      </button>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add components/chat/ChatInput.tsx
git commit -m "feat(chat): composant ChatInput avec envoi Enter/bouton"
```

---

### Task 8: ChatShell + page route

**Files:**
- Create: `components/chat/ChatShell.tsx`
- Create: `app/trips/[tripId]/chat/page.tsx`

- [ ] **Step 1: Implémenter ChatShell**

```tsx
'use client'

import { MessageList } from './MessageList'
import { ChatInput } from './ChatInput'

interface ChatShellProps {
  tripId: string
  currentUserId: string
}

export function ChatShell({ tripId, currentUserId }: ChatShellProps) {
  return (
    <div className="flex flex-col h-[calc(100dvh-5rem)] md:h-dvh">
      <div className="px-4 py-3 border-b border-hairline dark:border-hairline-dark">
        <h1 className="mk-display text-lg">Chat</h1>
      </div>
      <MessageList tripId={tripId} currentUserId={currentUserId} />
      <ChatInput tripId={tripId} userId={currentUserId} />
    </div>
  )
}
```

`h-[calc(100dvh-5rem)]` sur mobile pour compenser la bottom tab (80px). `h-dvh` sur desktop car pas de bottom tab.

- [ ] **Step 2: Implémenter la page route**

Créer `app/trips/[tripId]/chat/page.tsx` :

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { ChatShell } from '@/components/chat/ChatShell'

export default async function ChatPage({
  params,
}: {
  params: Promise<{ tripId: string }>
}) {
  const { tripId } = await params
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/welcome')

  return <ChatShell tripId={tripId} currentUserId={user.id} />
}
```

- [ ] **Step 3: Vérifier que la page charge**

Run: `npm run dev`
Naviguer vers `/trips/{un-trip-id}/chat`.
Expected: La page affiche le header "Chat", le message vide "Aucun message. Lancez la conversation !" et le champ de saisie.

- [ ] **Step 4: Commit**

```bash
git add components/chat/ChatShell.tsx app/trips/[tripId]/chat/page.tsx
git commit -m "feat(chat): ChatShell + page route /trips/[tripId]/chat"
```

---

### Task 9: Navigation — ajouter tab Chat

**Files:**
- Modify: `app/trips/[tripId]/(nav)/side-rail.tsx`
- Modify: `app/trips/[tripId]/(nav)/bottom-tab.tsx`

- [ ] **Step 1: Ajouter le tab dans SideRail**

Dans `side-rail.tsx`, ajouter `MessageSquare` à l'import lucide :

```typescript
import { BookOpen, Calendar, Home, Map, MessageSquare, Plus, Wallet } from 'lucide-react'
```

Ajouter l'entrée dans le tableau `NAV` (entre Split et Guide) :

```typescript
const NAV = [
  { Icon: Home, label: 'Home', suffix: '' },
  { Icon: Map, label: 'Map', suffix: 'map' },
  { Icon: Calendar, label: 'Planning', suffix: 'planning' },
  { Icon: Wallet, label: 'Split', suffix: 'budget' },
  { Icon: MessageSquare, label: 'Chat', suffix: 'chat' },
  { Icon: BookOpen, label: 'Guide', suffix: 'guide' },
] as const
```

- [ ] **Step 2: Ajouter le tab dans BottomTab**

Dans `bottom-tab.tsx`, ajouter `MessageSquare` à l'import lucide :

```typescript
import { BookOpen, Calendar, Home, Map, MessageSquare, Wallet } from 'lucide-react'
```

Ajouter l'entrée dans le tableau `TABS` (entre Split et Guide) :

```typescript
const TABS = [
  { id: 'home', Icon: Home, label: 'Home', suffix: '' },
  { id: 'map', Icon: Map, label: 'Map', suffix: 'map' },
  { id: 'plan', Icon: Calendar, label: 'Planning', suffix: 'planning' },
  { id: 'split', Icon: Wallet, label: 'Split', suffix: 'budget' },
  { id: 'chat', Icon: MessageSquare, label: 'Chat', suffix: 'chat' },
  { id: 'guide', Icon: BookOpen, label: 'Guide', suffix: 'guide' },
] as const
```

- [ ] **Step 3: Vérifier visuellement**

Run: `npm run dev`
Expected: Le tab "Chat" apparaît dans la sidebar desktop et la barre mobile, avec l'icône MessageSquare. Cliquer dessus navigue vers `/trips/{id}/chat`.

- [ ] **Step 4: Commit**

```bash
git add app/trips/[tripId]/(nav)/side-rail.tsx app/trips/[tripId]/(nav)/bottom-tab.tsx
git commit -m "feat(chat): ajouter tab Chat dans SideRail + BottomTab"
```

---

### Task 10: Régénérer les types Supabase

**Files:**
- Modify: `lib/supabase/types.ts`

- [ ] **Step 1: Appliquer la migration sur le remote Supabase**

Copier le contenu de `supabase/migrations/20260524000000_chat_messages.sql` et l'exécuter dans le **SQL Editor** du dashboard Supabase (même méthode que la migration `day_id` précédente).

- [ ] **Step 2: Régénérer les types**

Run: `npm run db:types`
Expected: Le fichier `lib/supabase/types.ts` est mis à jour avec la table `messages` (colonnes id, trip_id, user_id, body, created_at, updated_at).

- [ ] **Step 3: Vérifier que le build TypeScript passe**

Run: `npx tsc --noEmit`
Expected: Aucune erreur. Les types `Tables['messages']` sont maintenant disponibles et `LocalMessage` dans `schema.ts` est correctement typé.

- [ ] **Step 4: Commit**

```bash
git add lib/supabase/types.ts
git commit -m "chore: régénérer types Supabase avec table messages"
```

---

### Task 11: Test E2E

**Files:**
- Create: `e2e/chat.spec.ts`

- [ ] **Step 1: Écrire le test E2E**

```typescript
import { test, expect } from '@playwright/test'

test.describe('Chat du trip', () => {
  test('envoyer un message et le voir apparaître', async ({ page }) => {
    // Naviguer vers un trip existant (suppose un seed ou un trip déjà créé)
    await page.goto('/trips')
    const tripLink = page.getByRole('link').filter({ hasText: /trip/i }).first()
    await tripLink.click()

    // Aller au chat
    await page.getByRole('link', { name: 'Chat' }).first().click()
    await expect(page).toHaveURL(/\/chat$/)

    // Vérifier l'état vide
    await expect(page.getByText('Aucun message')).toBeVisible()

    // Envoyer un message
    const input = page.getByPlaceholder('Message...')
    await input.fill('Hello depuis le test E2E !')
    await page.getByRole('button').filter({ has: page.locator('svg') }).last().click()

    // Le message apparaît
    await expect(page.getByText('Hello depuis le test E2E !')).toBeVisible()

    // L'état vide a disparu
    await expect(page.getByText('Aucun message')).not.toBeVisible()
  })
})
```

- [ ] **Step 2: Lancer le test**

Run: `npx playwright test e2e/chat.spec.ts`
Expected: PASS (nécessite un utilisateur connecté et un trip existant — adapter les fixtures si besoin).

- [ ] **Step 3: Commit**

```bash
git add e2e/chat.spec.ts
git commit -m "test(chat): E2E envoi de message"
```

---

### Task 12: Test manuel complet

- [ ] **Step 1: Test envoi + réception temps réel**

1. Ouvrir l'app dans 2 onglets avec le même trip (idéalement 2 comptes différents)
2. Envoyer un message dans l'onglet 1
3. Vérifier que le message apparaît en temps réel dans l'onglet 2
4. Vérifier que le message de l'onglet 1 est aligné à droite (isMine)
5. Vérifier que le message dans l'onglet 2 est aligné à gauche avec le nom de l'auteur

- [ ] **Step 2: Test offline**

1. Couper le réseau (DevTools → Network → Offline)
2. Envoyer un message → il doit apparaître localement (via Dexie)
3. Remettre le réseau
4. Le message doit se synchroniser (le `_pending_mutation_id` disparaît)

- [ ] **Step 3: Test mobile**

1. Ouvrir sur un écran mobile (DevTools → responsive)
2. Vérifier que la bottom tab affiche l'icône Chat
3. Vérifier que le chat scroll bien, que le clavier ne cache pas l'input
4. Vérifier que `100dvh` compense correctement la bottom tab

---

## Récapitulatif

| Task | Description | Effort |
|---|---|---|
| 1 | Migration SQL (table + RLS + realtime) | 5 min |
| 2 | Types TS + Dexie v3 | 5 min |
| 3 | Hydrate + Realtime | 3 min |
| 4 | Mutations | 3 min |
| 5 | MessageBubble + tests | 10 min |
| 6 | MessageList | 5 min |
| 7 | ChatInput | 5 min |
| 8 | ChatShell + page route | 5 min |
| 9 | Navigation tabs | 3 min |
| 10 | Régénérer types Supabase | 5 min |
| 11 | Test E2E | 5 min |
| 12 | Test manuel | 10 min |
| **Total** | | **~65 min** |
